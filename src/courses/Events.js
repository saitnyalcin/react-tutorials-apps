import React from 'react';

const Events = () => {
  return (
    <div>
      <h3>
        React events are named using camelCase, rather than lowercase. With JSX
        you pass a function as the event handler, rather than a string.
      </h3>
    </div>
  );
};

export default Events;
